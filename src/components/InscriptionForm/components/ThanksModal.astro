<section id="thanks-modal">
    <article class="content">
        <img
            src="/images/ICON_REGISTERED.png"
            alt="Imagen Modal Inscripcion"
        />
        <h2>¡Gracias por enviarnos tus datos!</h2>
        <p>Nos pondremos en contacto contigo en breve</p>
        <div class="home-btn">
            <a href="/">VOLVER AL INICIO</a>
        </div>
    </article>
</section>

<style lang="scss">
    #thanks-modal {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        z-index: 15;
        display: grid;
        place-items: center;
        background-color: #00000095;
        visibility: hidden;
        opacity: 0;
        transition: 0.3s;
        &.open {
            visibility: visible;
            opacity: 1;
        }
    }
    .content {
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        text-align: center;
        gap: 20px;
        background-color: var(--white);
        padding: 100px;
        border-radius: 10px;
        @media (max-width: 925px) {
            padding: 80px 40px;
        }
        @media (max-width: 760px) {
            padding: 60px 25px;
        }
        @media (max-width: 600px) {
            padding: 55px 15px;
        }
        @media (max-width: 500px) {
            padding: 35px 10px;
        }
        img {
            width: 120px;
            translate: 15px 0;
            @media (max-width: 500px) {
                width: 90px;
            }
        }
        h2 {
            color: var(--primary);
            font-size: 40px;
            @media (max-width: 760px) {
                font-size: 32px;
            }
            @media (max-width: 600px) {
                font-size: 27px;
            }
            @media (max-width: 500px) {
                font-size: 20px;
            }
        }
        p {
            font-size: 24px;
            @media (max-width: 760px) {
                font-size: 20px;
            }
            @media (max-width: 600px) {
                font-size: 18px;
            }
            @media (max-width: 500px) {
                font-size: 14px;
            }
        }
        & .home-btn {
            display: flex;
            justify-content: center;
            a {
                font-size: 24px;
                text-decoration: none;
                padding: 15px 35px;
                border-radius: 5px;
                color: var(--white);
                background-color: var(--primary);
                font-weight: 600;
                border: 3px solid var(--primary);
                transition: 0.2s;
                user-select: none;
                &:hover {
                    color: var(--fifth-color);
                }
                @media (max-width: 760px) {
                    font-size: 22px;
                }
                @media (max-width: 500px) {
                    font-size: 18px;
                }
            }
        }
    }
</style>
