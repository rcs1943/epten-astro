<article class="media-section">
    <a href="./inscripcion" class="signup-btn">Inscríbete</a>
    <div class="icons">
        <a href="https://wa.me/+51958634825" target="_blank">
            <iconify-icon
                icon="akar-icons:whatsapp-fill"
            ></iconify-icon>
        </a>
        <a
            href="https://www.facebook.com/eptenn"
            target="_blank"
        >
            <iconify-icon icon="ei:sc-facebook"> </iconify-icon>
        </a>
        <a
            href="https://www.instagram.com/eptensac/"
            target="_blank"
        >
            <iconify-icon
                icon="akar-icons:instagram-fill"
            ></iconify-icon>
        </a>
    </div>
</article>

<style lang="scss">
    .media-section {
        display: flex;
        flex-direction: column;
        justify-content: center;
        gap: 30px;
        max-width: 225px;
        @media (max-width: 900px) {
            max-width: 400px;
        }
        @media (max-width: 500px) {
            padding: 0 40px;
            max-width: unset;
        }
    }
    .signup-btn {
        display: flex;
        justify-content: center;
        padding: 10px 60px;
        font-size: 22px;
        font-weight: 600;
        border-radius: 5px;
        transition: 0.25s;
        &:first-child {
            color: var(--primary);
            background-color: #fcd900;
        }
        &:hover {
            background-color: var(--white);
            color: var(--black);
        }
    }
    .icons {
        display: flex;
        justify-content: center;
        gap: 20px;
        font-size: 24px;
        a {
            display: flex;
            align-items: center;
            justify-content: center;
            color: var(--primary);
            background-color: var(--white);
            padding: 6px;
            border-radius: 50%;
            transition: 0.2s;
            &:hover {
                color: var(--white);
                background-color: var(--black);
            }
        }
    }
</style>